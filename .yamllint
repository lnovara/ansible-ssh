extends: default

rules:

  # Disable line-length and truthy values reporting
  line-length: disable
  truthy: disable

  # Max 1 space to separate the elements in brakets
  braces:
    max-spaces-inside: 1
    level: error

  # Max 1 space in brackets
  brackets:
    max-spaces-inside: 1
    level: error
